#include <stdio.h>
#include <stdlib.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int b;

	for (b = 0; b < 10; ++b)
		printf("%d", b);

	putchar('\n');

	return (0);
}
